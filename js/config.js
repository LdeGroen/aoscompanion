// URL van de AppSync-backend (https://github.com/LdeGroen/appsync, draait op de energiepi).
// Leeg = app werkt puur lokaal (localStorage, accounts per apparaat).
export const API_URL = "https://apps.lucdegroen.nl";

// Sleutel waaronder deze app zijn data opslaat in de backend.
export const APP_KEY = "aoscompanion";
